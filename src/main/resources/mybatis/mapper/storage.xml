<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="storage">
	<select id="getList" parameterType="String" resultType="Storage">
		select * from file_system where users_id = #{value} and fs_pid is null 
	</select>
	
	<select id="moveSubDir" parameterType="Storage" resultType="Storage">
		select * from file_system where users_id = #{users_id} and fs_pid = #{fs_uid}
	</select>
	
	<select id="moveParentDir" parameterType="Storage" resultType="Storage">
		select * from file_system where users_id = #{users_id} and fs_uid = #{fs_pid}
	</select>
</mapper>



